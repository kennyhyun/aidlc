# aidlc script environment variables configuration example
# Usage: cp .env.example .env and modify the values as needed

# Jira settings (used by fetch-tickets.sh, etc.)
JIRA_URL=https://your-company.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-jira-api-token

# GitLab settings
GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=your-gitlab-token
GITLAB_RO_TOKEN=your-readonly-token

# for aidlc/scripts/check-gitlab-resources.sh
# GitLab project settings (used by check-gitlab-resources.sh, get_diff.sh, etc.)
# Project ID should be URL-encoded (e.g., group%2Fsubgroup%2Fproject)
GITLAB_RUNNER_PROJECT_ID=your-group%2Fyour-project
# Project ID which has Kubernetes agaent
GITLAB_AGENT_PROJECT_ID=your-group%2Fyour-project

# GitLab group settings (used by check-group-resources.sh)
GITLAB_GROUP_PATH=your-group%2Fyour-subgroup
GITLAB_GROUP_ID=2

# MR review settings (used by get_diff.sh)
# Project ID (numeric) to fetch MR changes from
GITLAB_MR_PROJECT_ID=1


