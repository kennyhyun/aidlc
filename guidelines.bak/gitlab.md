Our Gitlab URL:

https://gitlab.imaginetoolbox.com/

imagine-online project id: 5

Set up tokens in `.zshrc` or `.profile`:

```bash
export GITLAB_RO_TOKEN="your-personal-api-token"
```

or use GITLAB_TOKEN if exists