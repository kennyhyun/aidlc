Our Gitlab URL:

https://gitlab.com/

imagine-online project id: 1

Set up tokens in `.zshrc` or `.profile`:

```bash
export GITLAB_TOKEN="your-personal-api-token"
export GITLAB_RO_TOKEN="your-personal-ro-api-token"
```

and use GITLAB_TOKEN or GITLAB_RO_TOKEN
